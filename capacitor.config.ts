import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'videojs-playback-rates-flicker',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
